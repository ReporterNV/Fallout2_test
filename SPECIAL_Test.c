#include "SPECIAL_function.h"

int main()
{

	struct Person_list *start = NULL;
	struct Person_list *current = NULL;
	uct SPECIAL));

	outputPerson_list(start);
	puts("You result in file SPECIAL\n");
	releasePerson_list(c_list.next);
	return 0;
}

	// Person a={"TEA",   1, 'M', {1,1,1,1,1,1,1}};
	// Person b={"Cake", 12, 'f', {5,5,5,5,5,5,5}};
	// Person c={"Main", 41, 'e', {4,2,1,6,8,4,5}};
	// Person_list a_list = {a, NULL};
	// Person_list b_list = {b, NULL};
	// Person_list c_list = {c, NULL};

	// a_list.next = &b_list;
	// b_list.next = &c_list;

	// current = createPerson();
	// start = current;
	// current = current -> next;
	// current = createPerson();
	// printf("%d\n",sizeof(struct Person));
	// printf("%d\n",sizeof(str
